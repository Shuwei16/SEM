<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta content="width=device-width, initial-scale=1.0" name="viewport">

    <title>Search FOCS Programme</title>
    <meta content="search FOCS Programmes" name="description">
    <meta content="search FOCS Programmes" name="keywords">

    <link href="/images/tarIco.ico" rel="icon" type="image/x-icon">

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">

        <!-- ======================================================
  * Member: Teoh Xin Zhi (2205704)
            Leau Goon Hei (2205668)
            Heah Jun Xi (2205650)
            Wang Shu Wei (2205708)
            Mah Jia Yi (2205684)
  ======================================================== -->

</head>
<body>
    <header class="my-5 mx-auto">
        <img src="/images/tarumt-logo.png" class="w-25 rounded mx-auto d-block">
        <h1 class="text-center">Search FOCS Programme</h1>
    </header>

    <div class="text-center">
        <div class="search-container">
          <form id="searchForm" action="/searchResult" method="POST">
            <input type="text" placeholder="Search.." name="search" id="searchText">
            <button type="submit" class="btn btn-primary" style="background-color: #ECA500;border-color: #ECA500;">Submit</button>
          </form>
        </div>
    </div>

    <!-- Container to hold the result table -->
    <div id="searchResultContainer" class="container"></div>

    <div class="text-center mt-5">
        <a href="/"><button class=" btn btn-primary " style="background-color: #ECA500;border-color: #ECA500;">Back</button></a>
    </div>
</body>
</html>


<script type="text/javascript">
    document.getElementById('searchForm').addEventListener('submit', async (event) => {
        event.preventDefault(); // Prevent the default form submission behavior
    
        // Retrieve the selected courses from the dropdown lists
        const searchText = document.getElementById('searchText').value;
        
        // Make a POST request to your /result endpoint with course1 and course2 values
        const response = await fetch('/searchResult', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
            },
            body: JSON.stringify({searchText}),
        });
    
        if (response.ok) {
            // Handle the response, e.g., display the result on the web page
            const data = await response.text();
            document.getElementById('searchResultContainer').innerHTML = data;
        } else {
            console.error('Error:', response.status, response.statusText);
        }
    });
</script>